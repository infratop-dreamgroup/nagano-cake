<div class="container pt-5">
  <div class="row">
    <div class="col-4">
      <!--後程部分ﾃﾝﾌﾟﾚ-->
      <table>
        <thead>
          <tr>
            <th>ジャンル一覧</th>
          </tr>
        </thead>
        <tbody>
          <tr></tr>
        </tbody>
      </table>


</div>
    <div class="col-8">
      <h2>商品一覧</h2>
       <% @items.each do |item| %>
       <%= image_tag item.image(100,100) %>
       <% item.name %>
       <% item.price %>
       <% end %>


    </div>
  </div>
</div>